# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

**Receipt Board** is a **desktop application**. As of this file's creation the repository is freshly initialized and contains **no application code yet** — only the standard init scaffolding (`.gitignore`, `README.md`, this file, `MEMORY.md`).

There is therefore no build, lint, test, or run command to document yet, and no architecture to describe. The sections below are placeholders to be filled in once the tech stack is chosen and the first code lands.

> When you add real tooling and structure, replace the placeholder sections below with concrete commands and an architecture overview, and update `README.md` to match. Do not document commands or architecture that do not exist yet.

## Tech stack

_Undecided._ (Desktop app — e.g. Electron, Tauri, or native. Confirm with the user before scaffolding.)

## Commands

_None yet._ Populate with build, lint, test (including how to run a single test), and run/dev commands once tooling is added.

## Architecture

_None yet._ Populate with the high-level structure once code exists.
